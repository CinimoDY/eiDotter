// Tailwind v4 — the PostCSS plugin moved to @tailwindcss/postcss.
// Vendor prefixing and @import handling are built in (no autoprefixer).
module.exports = {
  plugins: {
    '@tailwindcss/postcss': {},
  },
};
