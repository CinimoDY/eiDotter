. {
  display: block;
  font-family: var(--dos-global-typography-fonts-dos), var(--dos-global-typography-fonts-fallback);
  color: var(--dos-global-colors-dos-yellow);
}

/* Variants */
..small {
  font-size: 0.875rem;
}

..medium {
  font-size: 1rem;
}

..large {
  font-size: 1.25rem;
}

/* States */
..hover {
  opacity: 0.8;
}

..active {
  opacity: 0.6;
}

..disabled {
  opacity: 0.4;
  pointer-events: none;
}

..selected {
  border: 1px solid var(--dos-global-colors-dos-yellow);
}

..filled {
  background-color: var(--dos-global-colors-dos-yellow);
  color: var(--dos-global-colors-dos-black);
}
